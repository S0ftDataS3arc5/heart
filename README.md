# I love you baby — simple static site

Run the tiny static server and open the site in your browser:

```bash
python3 server.py
# then open http://localhost:8000
```

Files:
- index.html — main page
- styles.css — styles and glow effects
- script.js — animation logic
- server.py — static server
